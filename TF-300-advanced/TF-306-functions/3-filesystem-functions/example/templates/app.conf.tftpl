# Application Configuration
# Generated by Terraform — do not edit manually
# Environment: ${environment}

[server]
host        = ${host}
port        = ${port}
environment = ${environment}

[features]
${features_list}

[database]
host     = ${db_host}
port     = ${db_port}
db_name  = ${db_name}